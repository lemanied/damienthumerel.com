---
---

<div class="ascii-divider" aria-hidden="true"></div>

<style>
  .ascii-divider {
    color: var(--border);
    margin: var(--space-2) 0;
    overflow: hidden;
    line-height: 1;
    height: 1em;
  }
  .ascii-divider::before {
    content: "────────────────────────────────────────────────────────────────────────────────";
    color: var(--border);
    display: block;
    white-space: nowrap;
    overflow: hidden;
  }
</style>
